
<div id="page-login">
    <header class="header">
        <div id="joinLogoLink" class="header-item logo"><h1><b>Chat</b>App!</h1></div>
        <div class="header-center">&nbsp;</div>
        <div class="header-item"><!-- menu will go here --></div>
    </header>
    <div class="login">

        <div id="joinSection" class="form-control join">
            <div class="user-input">
                <h1 class="title">Join A Chat</h1>
                <form id="joinForm" method="post" action="/join">
                    <span class="input-label">What's your nickname?</span>
                    {{# if errors }}
                        {{#each errors }}
                            {{#when this.param 'eq' 'username'}}
                                <p class="error-message">{{ this.msg }}</p>
                            {{/when}}
                        {{/each}}
                    {{/if}}
                    <input id="usernameInputJoin" class="form-input-rounded" type="text" name="username" {{# if this.username }}value="{{this.username}}"{{/if}} required>

                    <span class="input-label">Email Address</span>
                    {{# if errors }}
                        {{#each errors }}
                            {{#when this.param 'eq' 'email'}}
                                <p class="error-message">{{ this.msg }}</p>
                            {{/when}}
                        {{/each}}
                    {{/if}}
                    <input id="emailInputJoin" class="form-input-rounded" type="text" name="email" {{# if this.email }}value="{{this.email}}"{{/if}} required>

                    <span class="input-label">Room ID</span>
                    {{# if errors }}
                        {{#each errors }}
                            {{#when this.param 'eq' 'room'}}
                                <p class="error-message">{{ this.msg }}</p>
                            {{/when}}
                        {{/each}}
                    {{/if}}
                    <input id="roomInputJoin" class="form-input-rounded break-after" type="text" name="room" {{# if this.room }}value="{{this.room}}"{{/if}} required>
                    <br>
                    <button type="submit" id="usernameBtn" class="btn">Join Chat</button>

                </form>
            </div>
        </div>
    </div>
</div>
<script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
<script src="/js/join.js"></script>